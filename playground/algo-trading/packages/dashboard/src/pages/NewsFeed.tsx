/**
 * NewsFeed page — headlines with FinBERT sentiment scores per ticker.
 * Full implementation in Sub-Task 10.
 */
export default function NewsFeed() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-semibold text-zinc-100">News Sentiment Feed</h1>
      <p className="text-sm text-zinc-400">
        Full implementation in Sub-Task 10 — FinBERT-scored headlines, ticker filters.
      </p>
    </div>
  );
}

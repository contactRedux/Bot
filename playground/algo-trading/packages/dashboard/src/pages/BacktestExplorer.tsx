/**
 * BacktestExplorer page — configure and run backtests, view results.
 * Full implementation in Sub-Task 10.
 */
export default function BacktestExplorer() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-semibold text-zinc-100">Backtest Explorer</h1>
      <p className="text-sm text-zinc-400">
        Full implementation in Sub-Task 10 — strategy selector, date range, results charts.
      </p>
    </div>
  );
}

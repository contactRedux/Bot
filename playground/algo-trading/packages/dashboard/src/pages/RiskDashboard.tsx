/**
 * RiskDashboard page — VaR, CVaR, drawdown gauge, correlation heatmap.
 * Full implementation in Sub-Task 10.
 */
export default function RiskDashboard() {
  return (
    <div className="space-y-4">
      <h1 className="text-xl font-semibold text-zinc-100">Risk Dashboard</h1>
      <p className="text-sm text-zinc-400">
        Full implementation in Sub-Task 10 — VaR/CVaR, drawdown gauge, correlation heatmap.
      </p>
    </div>
  );
}
